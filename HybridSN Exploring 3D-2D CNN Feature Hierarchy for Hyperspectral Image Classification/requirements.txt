# HybridSN / hyperspectral image classification (PyTorch)
#
# Install PyTorch separately so you can choose the correct CUDA build for your machine:
# https://pytorch.org/get-started/locally/
# Example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# Then install the rest with:
#   pip install -r requirements.txt

numpy>=1.26,<3
scipy>=1.13,<2
scikit-learn>=1.5,<2
scikit-image>=0.24,<1
matplotlib>=3.9,<4
tqdm>=4.66,<5
spectral>=0.23,<1
imageio>=2.34,<3
joblib>=1.4,<2
h5py>=3.11,<4

torchinfo>=1.8,<2
